{
    "private": true,
    "name": "tailwind-css-docker-playground",
    "version": "1.0.0",
    "description": "Tailwind CSS Docker Playground is a container based playground with live reload for learning Tailwind CSS.",
    "author": "Chris Lentz (https://github.com/chrislentz)",
    "license": "MIT",
    "scripts": {
        "dev": "live-server /var/www/public --port=9000 --host=0.0.0.0",
        "watch": "npx tailwindcss -i ./input.css -o ./public/main.css --watch",
        "build": "npx tailwindcss -i ./input.css -o ./public/main.css"
    },
    "devDependencies": {
        "tailwindcss": "^3.0.24"
    }
}
